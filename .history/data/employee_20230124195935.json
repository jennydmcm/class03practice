export default function Card({
    degree="none",
    colour= "pink",
    font="0",
}) {
    return (
        <div style={{backgroundColor: colour, fontSize: font}}>
            {
                colour === "blue" ? <img src={'icon/business.png'} width='100' height='100'/> :
                colour === "pink" ? <img src={'icon/computing.png'} width='100' height='100'/> :
                                    <img src={image} width='100'/>
            }
            {degree}
        </div>
    )
}